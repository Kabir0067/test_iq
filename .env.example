# Server
PORT=10000

# Recipient default (falls back to SMTP_USER if not set)
LOVER_EMAIL=

# SMTP settings (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
MAIL_FROM=
